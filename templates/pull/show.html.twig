{% extends 'base.html.twig' %}

{% block title %}Pull{% endblock %}

{% block body %}
    <h1>Pull</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pull.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pull.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ pull.prix }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ pull.marque }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pull.description }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ pull.stock }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ pull.status.value }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ pull.taille }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pull_index') }}">back to list</a>

    <a href="{{ path('app_pull_edit', {'id': pull.id}) }}">edit</a>

    {{ include('pull/_delete_form.html.twig') }}
{% endblock %}
