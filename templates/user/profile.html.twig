{% extends 'base.html.twig' %}

{% block title %}{% trans %}monProfil{% endtrans %}{% endblock %}

{% block body %}
<div class="container mt-5 mb-5">
    <h1 class="mb-4 text-center">{% trans %}monProfil{% endtrans %}</h1>

    <div class="row">
        <div class="col-md-4 mb-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="fas fa-user-circle me-2"></i>{% trans %}informations{% endtrans %}</h5>
                </div>
                <div class="card-body">
                    <h5 class="card-title">{{ user.prenom }} {{ user.nom }}</h5>
                    <p class="card-text text-muted">{{ user.email }}</p>
                    <p class="card-text">
                        {% for role in user.roles %}
                            <span class="badge bg-info text-dark">{{ role }}</span>
                        {% endfor %}
                    </p>
                </div>
            </div>
        </div>

        <div class="col-md-8">
            
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-secondary text-white">
                    <h5 class="mb-0"><i class="fas fa-map-marker-alt me-2"></i>{% trans %}mesAdresses{% endtrans %}</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        {% for adresse in user.adresses %}
                            <div class="col-md-6 mb-3">
                                <div class="card h-100 border-light bg-light position-relative">
                                    <div class="card-body">
                                        <p class="mb-1"><strong>Adresse #{{ loop.index }}</strong></p>
                                        <p class="mb-0">{{ adresse.rue }}</p>
                                        <p class="mb-0">{{ adresse.codePostal }} {{ adresse.ville }}</p>
                                        <p class="mb-0">{{ adresse.pays }}</p>
                                    </div>
                                    <a href="{{ path('app_adresse_edit', {'id': adresse.id}) }}" class="stretched-link" aria-label="{% trans %}modifierAdresse{% endtrans %}"></a>
                                </div>
                            </div>
                        {% else %}
                            <div class="col-12">
                                <p class="text-muted text-center my-3">{% trans %}aucuneAdresse{% endtrans %}</p>
                            </div>
                        {% endfor %}
                    </div>
                    <div class="text-center mt-3">
                        <a href="{{ path('app_adresse_new') }}" class="btn btn-outline-secondary btn-sm">
                            <i class="fas fa-plus me-1"></i>{% trans %}ajouterAdresse{% endtrans %}
                        </a>
                    </div>
                </div>
            </div>

            <div class="card shadow-sm mb-4">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0"><i class="fas fa-credit-card me-2"></i>{% trans %}mesCartes{% endtrans %}</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        {% for carte in user.cartesCredit %}
                            <div class="col-md-6 mb-3">
                                <div class="card h-100 border-light bg-light position-relative">
                                    <div class="card-body">
                                        <p class="mb-1 fs-5"><strong>{{ carte.numero|slice(0, 4) }} **** **** {{ carte.numero|slice(-4) }}</strong></p>
                                        <p class="mb-0 text-muted">Exp: {{ carte.dateExpiration|date('m/Y') }}</p>
                                    </div>
                                    <a href="{{ path('app_carte_credit_edit', {'id': carte.id}) }}" class="stretched-link" aria-label="{% trans %}modifierCarteCredit{% endtrans %}"></a>
                                </div>
                            </div>
                        {% else %}
                            <div class="col-12">
                                <p class="text-muted text-center my-3">{% trans %}aucuneCarte{% endtrans %}</p>
                            </div>
                        {% endfor %}
                    </div>
                    <div class="text-center mt-3">
                        <a href="{{ path('app_carte_credit_new') }}" class="btn btn-outline-info btn-sm">
                            <i class="fas fa-plus me-1"></i>{% trans %}ajouterCarteCredit{% endtrans %}
                        </a>
                    </div>
                </div>
            </div>

            <div class="card shadow-sm mb-4">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0"><i class="fas fa-shopping-bag me-2"></i>{% trans %}mesCommandes{% endtrans %}</h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>{% trans %}reference{% endtrans %}</th>
                                    <th>{% trans %}date{% endtrans %}</th>
                                    <th>{% trans %}status{% endtrans %}</th>
                                    <th>{% trans %}prixTotal{% endtrans %}</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for commande in commandes %}
                                <tr>
                                    <td>#{{ commande.reference }}</td>
                                    <td>{{ commande.date ? commande.date|date('d/m/Y') : '' }}</td>
                                    <td><span class="badge bg-secondary">{{ commande.statusCommande.value }}</span></td>
                                    <td>
                                        {% set total = 0 %}
                                        {% for produit in commande.produits %}
                                            {% set total = total + produit.prix %}
                                        {% endfor %}
                                        <strong>{{ total }} €</strong>
                                    </td>
                                </tr>
                                {% else %}
                                <tr>
                                    <td colspan="4" class="text-center py-4 text-muted">{% trans %}aucuneCommande{% endtrans %}</td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <div class="row mt-4">
        <div class="col-12 text-center">
            <hr class="mb-4">
            <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-lg">
                <i class="fas fa-sign-out-alt me-2"></i>{% trans %}deconnexion{% endtrans %}
            </a>
        </div>
    </div>
</div>
{% endblock %}
