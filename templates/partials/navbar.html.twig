<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">E-commerce</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_pull_index') }}">Pulls</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_t_shirt_index') }}">T-Shirt</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_pantalon_index') }}">Pantalons</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_short_index') }}">Shorts</a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        {# <a class="nav-link" href="{{ path('admin_dashboard') }}">Admin</a> #}
                    </li>
                {% endif %}

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
