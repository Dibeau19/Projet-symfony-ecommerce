<form method="post" action="{{ path('app_pantalon_delete', {'id': pantalon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pantalon.id) }}">
    <button class="btn">Delete</button>
</form>
