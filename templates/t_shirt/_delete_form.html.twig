<form method="post" action="{{ path('app_t_shirt_delete', {'id': t_shirt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_shirt.id) }}">
    <button class="btn">Delete</button>
</form>
