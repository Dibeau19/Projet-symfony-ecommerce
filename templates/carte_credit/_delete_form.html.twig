<form method="post" action="{{ path('app_carte_credit_delete', {'id': carte_credit.id}) }}" onsubmit="return confirm('{% trans %}etesVousSur{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte_credit.id) }}">
    <button class="btn btn-danger mt-3">{% trans %}supprimer{% endtrans %}</button>
</form>
